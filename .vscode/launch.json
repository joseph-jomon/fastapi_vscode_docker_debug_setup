{
    "configurations": [
        
        {
            "name": "Python Debugger: Remote Attach",
            "type": "debugpy",
            "request": "attach",
           // "preLaunchTask": "Docker Compose Up",
            "connect": {
                "host": "localhost",
                "port": 5678
            },
            "pathMappings": [
                {
                    "localRoot": "${workspaceFolder}/app_folder/app",
                    "remoteRoot": "/app"
                }
            ],
            "justMyCode": false,

        },
        
    ]
}